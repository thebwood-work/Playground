export interface IAddressSearchModel {
    id: string | null,
    streetNumber: string | null,
    streetName: string | null,
    city: string | null,
    stateId: string | null,
    zipCode: string | null
}