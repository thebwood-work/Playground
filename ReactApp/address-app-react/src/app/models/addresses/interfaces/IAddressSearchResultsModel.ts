export interface IAddressSearchResultsModel {
    id: string | null,
    streetNumber: string | null,
    streetName: string | null,
    city: string | null,
    stateId: string | null,
    stateName: string | null,
    zipCode: string | null,
}